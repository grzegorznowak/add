# Blocked: <change-slug>

<!-- This file is the explicit blocker gate. Its presence normally prevents
     lifecycle gates from continuing. The sole publication-rerun exception
     lets confirmed `/openspec-feedback` packet triage verify exact planned
     existing bytes without rewriting before Status-last BLOCKED publication.
     Otherwise remove this file to unblock; editing or annotating it may
     document resolution, but the gate remains closed while this file exists.

     Created or updated by `/openspec-story-claim` or
     `/openspec-story-resume`. Confirmed `/openspec-feedback` review-packet
     triage may create an absent confirmed file, treats exact existing
     bytes as satisfied, and stops without overwrite on conflict. Readonly
     implementation review uses this file only as an input gate. A plan-review
     blocked verdict uses the Plan lane instead. -->

## Blocker
<!-- Concrete description of what is blocked and why. -->

## Resolution
<!-- How the operator should resolve the blocker. Remove this file when resolved. -->
