## Pi Primitives

Use Pi runtime tools only for fresh, read-only review evidence gathering. Follow the canonical skill's artifact authority, state-bound handoff contract, and fresh-review firewall.

Pi declarative read-only is a sticky interactive-session guard, not a security sandbox. Invoke this skill in a fresh interactive Pi session so `readonly: true` activates. Bash protection is classifier-only and best-effort when OS sandboxing is unavailable; Task, custom, and MCP tools are not generically secured. Never use Task, custom tools, MCP tools, or delegated agents to mutate product or coordination state. Tests may run only when they succeed without worktree mutation.

After story resolution, use the canonical `<initiative_slug>` and `<story_slug>` in every child prompt.

### Fresh review firewall

Do not retrieve, import, quote, summarize, or pass through any notebook page/reference supplied by an implementation parent or converger. Do not reuse implementation/convergence research or ops pages under a renamed selector. If inherited notebook context or prior implementation-session framing is present, abort under the base skill's Oblivious Context Boundary before any child work.

A standalone prior review page `openspec-review-<initiative_slug>-<story_slug>` may be read only after the firewall passes and only as optional sourced orientation. Story artifacts and live evidence outrank it.

### Research before review

When broad context would be noisy, use a fresh child only for read-only codebase evidence. Give it current artifact paths, canonical slugs, acceptance/proof selectors, and live repo/worktree locations discovered independently in this review session. The child must return sourced findings directly, must not retrieve parent/converger notebooks or prior chat context, and must not decide the verdict or mutate files. Verify every material child claim with direct reads/search against cited anchors.

### Read-only review return

Return the canonical fenced `## Completed Review Handoff` in the response without writing, editing, persisting, replacing, normalizing, reconciling, or appending any artifact or notebook state. `/openspec-feedback` explicitly clears sticky Pi read-only state with `readonly: false`, validates the packet, and alone publishes completed-review artifacts.
