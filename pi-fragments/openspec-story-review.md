## Pi Runtime Boundary

Use only the canonical skill's read-only evidence tools and immutable packet contract. Pi-specific persistence and publication are unavailable to this evaluator. Do not retrieve inherited implementation-session memory or delegate review passes. Perform any required focused passes sequentially, verify primary evidence directly, and return only the canonical `ADD-REVIEW-PACKET/1` packet.
