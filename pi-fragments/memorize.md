## Pi Primitives

Use `ledger_add`, `ledger_list`.

This skill maps directly to `ledger_add`. After identifying friction points and patches:
`ledger_add("memorize-<topic>", "- <friction> (score: <N>/10)\n- Proposed patch: <description>")`.

Propose patches to AGENTS.md/stories/docs as before — confirm with operator before applying. The ledger entry survives session resets without file edits.
